nodes:
  - id: webcam_depth
    operator:
      python: ../webcam_depth.py
      inputs:
        tick: dora/timer/millis/50
      outputs:
        - image
    env:
      DEVICE_INDEX: 0

  - id: plot_depth
    operator:
      python: ./plot_depth.py
      inputs:
        image: webcam_depth/image